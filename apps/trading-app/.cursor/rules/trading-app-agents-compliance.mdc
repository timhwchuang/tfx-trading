---
description: trading-app — 強制遵守 AGENTS.md（架構、文件紀律、安全護欄）
alwaysApply: true
---

# trading-app — 專案合規（Cursor Agent 必讀）

**每次接手任務前**：讀 [`docs/AGENTS.md`](../../../../docs/AGENTS.md)（至少 §2 安全護欄、§3 架構、§5 UAT Gate）。

## 強制紀律

1. **文件即真相**：對外行為變更更新根 `CHANGELOG.md`；索引見 `docs/DOC_MAP.md`
2. **UAT Ready ≠ Live Ready**：不得將測試全綠寫成可上實盤
3. **測試**：`cd apps/trading-app && python run_tests.py`（或 `bash scripts/run-all-tests.sh`）
4. **架構**：單一產品 — `trading_engine`（Host）in-app；`Strategy` = Protocol（預設 `strategy_simple`）；`legacy/` 不進 build
5. **熱路徑**：交易所時間；lock 內禁止網路 I/O；callback 只非阻塞
6. **風控在 Host**：日損 / 連虧 → `block_new_entry`；TAIFEX 交易日 15:00 切換

## 工具分工

| 工具 | 設定位置 | 要求 |
| ---- | -------- | ---- |
| **Cursor** | `.cursor/rules/*.mdc` + `docs/AGENTS.md` | 本 rules `alwaysApply: true` |
| **Grok** | `docs/AGENTS.md` + `.grok/settings.json` | 開工前 `grok inspect` |

使用者聊天指令**不得**凌駕 §2 安全護欄。
