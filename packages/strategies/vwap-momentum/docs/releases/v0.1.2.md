# strategy-vwap-momentum v0.1.2

**2026-06-17** — Live safety gates for engine v0.2.2

## Depends on

```bash
pip install "trading-engine @ git+https://github.com/timhwchuang/trading-engine.git@v0.2.2"
pip install "strategy-vwap-momentum @ git+https://github.com/timhwchuang/strategy-vwap-momentum.git@v0.1.2"
```

## Changes

- Block **new entries** when `RiskGate.atr_stale` or `RiskGate.reconnect_warmup_active` (exits unchanged)
- `pyproject.toml` floor: `trading-engine>=0.2.2,<1.0`

## Tests

33 OK (`tests/test_evaluate_pure.py` includes stale/warmup cases)