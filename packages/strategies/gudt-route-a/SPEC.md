# strategy-gudt-route-a — Package SPEC

Feature contract: [`docs/features/gudt-route-a/SPEC.md`](../../../docs/features/gudt-route-a/SPEC.md).

Entry point: `gudt_route_a` → `GudtRouteAStrategy`.

Replay plans are built from counterfactual stack picks (see `integrations/gudt_replay_planner.py` in trading-app).
