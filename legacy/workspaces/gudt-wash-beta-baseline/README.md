# FT-023 gudt-wash-beta-baseline

Plugin backtest for **wash-day open→session flatten** sleeve (`gudt_wash_beta`). Independent from FT-021 Route A.

## Three-layer reports

| Layer | Script | Artifact |
|-------|--------|----------|
| Decision parity | `ft023_parity_check.py` | `reports/parity_report_{label}.json` |
| Kernel replay | `ft023_run_baseline.py` | `logs/baseline_{label}.log`, `reports/day_plans_{label}.json` |
| **Execution parity** | `ft023_execution_parity.py` | `reports/execution_parity_{label}.json` + `.md` |

Hard gate (execution): **CF round count == kernel round count** (`n` must match).

## Date slices

All `ft023_*` scripts share slice arguments (`reporting/date_slices.py`). Priority: `--from/--to` > `--months` > `--spot-check` > `--slice` > default `UAT_2m`.

**UAT blocker**: full **2026 H1** (`--from 2026-01-01 --to 2026-06-30`).

```bash
cd apps/trading-app/src

# Primary UAT gate (2026 H1)
FT003_HOLDOUT_UNSEAL=1 PYTHONPATH=. \
  python scripts/ft023_execution_parity.py --from 2026-01-01 --to 2026-06-30 --append-spot-log

# Daily smoke (UAT_2m)
FT003_HOLDOUT_UNSEAL=1 PYTHONPATH=. \
  python scripts/ft023_execution_parity.py

# Baseline + execution report
FT003_HOLDOUT_UNSEAL=1 PYTHONPATH=. \
  python scripts/ft023_run_baseline.py --execution-report --from 2026-01-01 --to 2026-06-30
```

Record spot-check results in `reports/SPOT_CHECK_LOG.md`.

## CF research ledger

Tune / holdout monthly SSOT: [`workspaces/gudt-baseline/WASH_BETA_LEDGER.md`](../gudt-baseline/WASH_BETA_LEDGER.md) (generated by `ft023_wash_beta_ledger.py`).

## GCE Live

```bash
CONFIG_PATH=/opt/tfx-trading/workspaces/gudt-wash-beta-baseline/config/config.yaml
```

See [`docs/ops/LinuxOps.md`](../../docs/ops/LinuxOps.md).
