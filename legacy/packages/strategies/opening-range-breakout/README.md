# FT-009 — Opening Range Breakout

Pre-registered: `range_minutes=30`, `buffer_atr_k=0.15`, first break only, ATR exits.

```bash
cd packages/strategies/opening-range-breakout
python run_tests.py
```
